<div class="container">
  <div class="row">
    <div class="col-sm-10 col-sm-offset-1">
      <div class="form-box">
        <h2>Add new ingredient for <strong><%= @cocktail.name %></strong></h2>
        <%= render 'form', cocktail: @cocktail, dose: @dose %>
      </div>

      <p><%= link_to "Back to Cocktail", cocktails_path, class: "btn btn-danger" %></p>
    </div>
  </div>
</div>
